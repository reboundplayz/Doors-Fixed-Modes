local plr = game.Players.LocalPlayer
local chr = plr.Character or plr.CharacterAdded:Wait()

--// Current room reference
local currentRoomNum = game.ReplicatedStorage.GameData.LatestRoom.Value
local currentRoom = workspace.CurrentRooms:FindFirstChild(tostring(currentRoomNum))

-- Replace your yellow room section with this

local TweenService = game:GetService("TweenService")

--// Current room reference
local currentRoomNum = game.ReplicatedStorage.GameData.LatestRoom.Value
local currentRoom = workspace.CurrentRooms:FindFirstChild(tostring(currentRoomNum))

-- Fade room into yellow after 2 sec
wait(2)

local function FadeRoomColor(room)
	if not room then return end

	for _, obj in pairs(room:GetDescendants()) do
		if obj:IsA("BasePart") then
			local tween = TweenService:Create(
				obj,
				TweenInfo.new(2, Enum.EasingStyle.Linear),
				{Color = Color3.fromRGB(246,238,0)}
			)
			tween:Play()

		elseif obj:IsA("PointLight") or obj:IsA("SpotLight") or obj:IsA("SurfaceLight") then
			local tween = TweenService:Create(
				obj,
				TweenInfo.new(2, Enum.EasingStyle.Linear),
				{Color = Color3.fromRGB(246,238,0)}
			)
			tween:Play()
		end
	end
end

FadeRoomColor(currentRoom)

-- wait until fade finishes, then spawn 2 sec later
wait(1)
wait(1)

-- Keep old room yellow permanently
require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game).caption("What the freak is that?!?!?!",true)

local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://85677794592741"
sound.Looped = false
sound.Parent = workspace
sound.Volume = 3
sound:Play()
game:GetService("Debris"):AddItem(sound,5)

wait(6)

if chr:GetAttribute("Crouching") then
	game.Players.LocalPlayer.Character.Humanoid.Health -= 1000
	game:GetService("ReplicatedStorage").GameStats["Player_"..game.Players.LocalPlayer.Name].Total.DeathCause.Value = "Bliss[Crouched Early]"
end

wait(2)

-- Replace ONLY your ImageGui section with this improved version

-- Replace ONLY your ImageGui section with this cleaner version

local Players = game:GetService("Players")
local player = Players.LocalPlayer

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ImageGui"
screenGui.IgnoreGuiInset = true
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local imageLabel = Instance.new("ImageLabel")
imageLabel.BackgroundTransparency = 1
imageLabel.AnchorPoint = Vector2.new(0.5,0.5)

-- centered and normal size
imageLabel.Position = UDim2.new(0.5,0,0.5,0)
imageLabel.Size = UDim2.new(1.5,0,1,0)

imageLabel.Image = "rbxassetid://106448471337051"
imageLabel.ImageTransparency = 0.65

-- keeps correct proportions (no horizontal stretch)
imageLabel.ScaleType = Enum.ScaleType.Fit

imageLabel.Parent = screenGui

task.delay(3,function()
	if screenGui then
		screenGui:Destroy()
	end
end)

task.delay(3,function()
	if screenGui then
		screenGui:Destroy()

		local achievementGiver = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

		achievementGiver({
			Title = "CROUCH NOW!!!",
			Desc = "Whatrrrrr I Do?",
			Reason = "Encounter Bliss",
			Image = "rbxassetid://97762043789701"
		})
	end
end)

wait(2)

if not chr:GetAttribute("Crouching") then
	game.Players.LocalPlayer.Character.Humanoid.Health -= 1000

	function GitAud(soundgit,filename)
		local url = soundgit
		local FileName = filename
		writefile(FileName..".mp3", game:HttpGet(url))
		return (getcustomasset or getsynasset)(FileName..".mp3")
	end

	local sound = Instance.new("Sound")
	sound.SoundId = GitAud("https://github.com/beanzjsjwj/doors-monsters-models/raw/refs/heads/main/bliss_sound.mp3","spawn")
	sound.Parent = workspace
	sound.Volume = 5
	sound:Play()
    game:GetService("Debris"):AddItem(sound,5)
end
