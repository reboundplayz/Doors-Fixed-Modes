-- REPLACE your existing hunger connection at the top with this:

local Kill = true

local hunger = game.ReplicatedStorage.GameData.LatestRoom.Changed:Connect(function()

    Kill = false

    game.Players.LocalPlayer.Character.Humanoid:TakeDamage(1000)

    game:GetService("ReplicatedStorage").GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "Nightmare Hunger [Opened Door]"

    -- FORCE STOP ALL ATTACK SOUNDS
    if workspace:FindFirstChild("Attacking1") then
        workspace.Attacking1:Stop()
        workspace.Attacking1:Destroy()
    end

    if workspace:FindFirstChild("AttackEnd") then
        workspace.AttackEnd:Stop()
        workspace.AttackEnd:Destroy()
    end

    if workspace:FindFirstChild("End") then
        workspace.End:Stop()
        workspace.End:Destroy()
    end

    if workspace:FindFirstChild("HungerScream") then
        workspace.HungerScream:Stop()
        workspace.HungerScream:Destroy()
    end

end)

local cue = Instance.new("Sound")

cue.Parent = game.Workspace
cue.Name = "HungerScream"
cue.SoundId = "rbxassetid://933230732"
cue.Volume = 10
cue.PlaybackSpeed = 1
cue:Play()

wait(17)

hunger:Disconnect()

-- Check current room
local entityModel = true
local latestRoom = game.ReplicatedStorage.GameData.LatestRoom.Value

if game.Workspace:FindFirstChild("SeekMovingNewClone") or 
   latestRoom == 50 or latestRoom == 100 or  
   (latestRoom >= 90 and latestRoom <= 100) then
    entityModel = false
end

local Kill = true

local depthsTer = game:GetObjects("rbxassetid://130473352149503")[1]
depthsTer.Parent = game.Workspace

workspace.HungerNew.Effects.Spawn:Destroy()

local part = depthsTer:FindFirstChildWhichIsA("BasePart") or depthsTer:FindFirstChildWhichIsA("Part")

part.CFrame = game.Workspace.CurrentRooms:GetChildren()[#game.Workspace.CurrentRooms:GetChildren() - 1].Parts.Floor.CFrame + Vector3.new(0, 6, 0)

wait(3)

-- =========================================================
-- 💀 KICK SYSTEM + GITHUB SOUND
-- =========================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local triggered = false

local function GitAud(soundgit, filename)
    local FileName = filename
    writefile(FileName..".mp3", game:HttpGet(soundgit))
    return (getcustomasset or getsynasset)(FileName..".mp3")
end

local function PlayKickSound()
    local sound = Instance.new("Sound")
    sound.Parent = workspace
    sound.Volume = 5
    sound.Looped = true

    local success, asset = pcall(function()
        return GitAud(
            "https://github.com/reboundplayz/doors-fear-mode-images/raw/refs/heads/main/ScreenRecording_07-04-2025%2016-09-19_1.mp3",
            "hunger_kick"
        )
    end)

    if success then
        sound.SoundId = asset
    else
        sound.SoundId = "rbxassetid://933230732"
    end

    sound:Play()
end

RunService.Heartbeat:Connect(function()
    if triggered then return end
    if not depthsTer or not depthsTer.Parent then return end

    local char = player.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end

    local hrp = char.HumanoidRootPart

    for _, v in pairs(depthsTer:GetDescendants()) do
        if v:IsA("BasePart") then
            if (hrp.Position - v.Position).Magnitude < 6 then
                triggered = true
                PlayKickSound()
                task.wait(0.2)
                player:Kick("You got too close to Nightmare Hunger...")
                return
            end
        end
    end
end)

-- REPLACE ONLY YOUR ATTACK LOOP WITH THIS (keeps fps freeze + fixes looping sound)

task.spawn(function()
    while Kill do
        task.wait(0.1)

        -- if next door opened stop everything instantly
        if game.ReplicatedStorage.GameData.LatestRoom.Value ~= latestRoom then
            Kill = false

            if workspace:FindFirstChild("Attacking1") then
                workspace.Attacking1:Stop()
                workspace.Attacking1:Destroy()
            end

            if workspace:FindFirstChild("AttackEnd") then
                workspace.AttackEnd:Stop()
                workspace.AttackEnd:Destroy()
            end

            break
        end

        local camera = workspace.CurrentCamera
        if not camera then return end

        local function IsLookingAt()
            local viewportPoint, onScreen = camera:WorldToViewportPoint(part.Position)
            if not onScreen then return false end

            local lookVector = camera.CFrame.LookVector
            local direction = (part.Position - camera.CFrame.Position).Unit
            return lookVector:Dot(direction) > 0.65
        end

        if not IsLookingAt() then

            -- FPS FREEZE EFFECT
            local FPS = 5
            local clock = tick()
            while tick() < clock + (1.6 / FPS) do end

            local humanoid = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid")
            if humanoid then
                humanoid:TakeDamage(10)
            end

            if not workspace:FindFirstChild("Attacking1") then
                local sound = Instance.new("Sound")
                sound.Name = "Attacking1"
                sound.Parent = workspace
                sound.Volume = 5
                sound.Looped = true
                sound.SoundId = GitAud(
    "https://github.com/reboundplayz/Doors-Fixed-Modes/raw/refs/heads/main/Video%20Project%202%201.mp3",
    "hunger_attack"
)
                sound:Play()
            end

        else
            if workspace:FindFirstChild("Attacking1") then
                workspace.Attacking1:Stop()
                workspace.Attacking1:Destroy()

                local endSound = Instance.new("Sound")
                endSound.Name = "AttackEnd"
                endSound.Parent = workspace
                endSound.Volume = 5
                endSound.Looped = false
                endSound.SoundId = "rbxassetid://6664373487"
                endSound:Play()

                game:GetService("Debris"):AddItem(endSound, 10)
            end
        end
    end
end)

game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()

local achievementGiver = loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"
))()

achievementGiver({
    Title = "Let Me Open The Door!",
    Desc = "Don't crash my game",
    Reason = "Encounter Nightmare Hunger",
    Image = "rbxassetid://134752454471771"
})

function CustomGitSound(soundlink, vol, filename)

    local sound = Instance.new("Sound")

    sound.SoundId = GitAud(soundlink, filename)

    sound.Parent = workspace

    sound.Name = "End"

    sound.Volume = 5

    sound.Looped = false

    sound:Play()

end

CustomGitSound(
    "https://github.com/reboundplayz/doors-fear-mode-images/raw/refs/heads/main/ScreenRecording_07-04-2025%2016-09-19_1.mp3",
    1,
    "leave"
)

-- delete sounds when next door opens
if workspace:FindFirstChild("Attacking1") then
    workspace.Attacking1:Destroy()
end
if workspace:FindFirstChild("AttackEnd") then
    workspace.AttackEnd:Destroy()
end

Kill = false

depthsTer:Destroy()
